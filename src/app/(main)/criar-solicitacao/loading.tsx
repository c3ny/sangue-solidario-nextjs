import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      title="Carregando Formulário"
      subtitle="Preparando formulário de solicitação de doação..."
    />
  );
}
