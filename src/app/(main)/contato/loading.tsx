import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      title="Carregando Página de Contato"
      subtitle="Preparando formulário de contato..."
    />
  );
}
