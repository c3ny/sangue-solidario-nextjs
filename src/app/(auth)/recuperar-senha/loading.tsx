import { PageLoading } from "@/components/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      title="Carregando Recuperação de Senha"
      subtitle="Preparando formulário de recuperação..."
    />
  );
}
